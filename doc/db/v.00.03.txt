drop table `users`;

CREATE TABLE `users` (
  `user_id` int(10) unsigned NOT NULL AUTO_INCREMENT,
  `email` varchar(255) NOT NULL,
  `name` varchar(255) NOT NULL,
  `created` datetime NOT NULL,
  PRIMARY KEY (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;


insert into `users` (`user_id`, `email`, `name`, `created`) 
values (1, 'adam@bednarski.pl', 'Adam Bednarski', now()); 

insert into users (user_id, email, name, created) 
values (2, 'philip@murphy.ie', 'Philip Murphy', now()); 

insert into users (user_id, email, name, created) 
values (3, 'mark@montgomerie.nz', 'Mark Montgomerie', now());

